### やり取りのルール

* 入力が不明確な場合は、確認の質問をすること。
* タスクが実現不可能な場合は、理由を説明し代替案を提案すること。
* 構造化された読みやすいフォーマット（見出し、リスト、コードブロック）を使用すること。
* 指示に忠実に従い、行ったことを明確に説明すること。
* 現在のタスクに関係のないコードを変更しないこと。
* 編集対象のコードのスタイルに常に合わせること。

### コーディング規約

* すべてのコードに対して、アサーション付きの意味のあるテストを書くこと。
* テストのアサーションを重複させないこと。
* テストカバレッジを継続的に向上させること。
* シンプルな設計の4つのルールを適用すること：

  1. コードが動作する（テストに合格する）。
  2. 意図が明確である。
  3. 重複がない。
  4. 要素が最小限である。

* 関数型スタイルを優先すること：

  * 明示的なパラメータを使用する。
  * 不変性を優先する。
  * 命令型より宣言型を優先する。
  * 状態を最小限にする。

### アーキテクチャ

* 技術的なレイヤーではなく、関心事ごとにモジュール化する。
* 1モジュールにつき1つの責務。
* モジュール間の結合度を低く保つ。
* 関数は短く、過剰な設計をしない。

### ワークフロー

* コーディング前に `spec.md` を読むこと。
* タスク完了後に `spec.md` を更新すること（変更を記録する）。
* 完了前にテストを書き、合格させること。
* セットアップ・実行情報を記載した `README.md` を維持すること。

  * すべてのドキュメント・仕様はMarkdownで管理すること。

### コミット戦略

* 1プロンプト = 1コミット。
* 各コミットは：
  * 自己完結していること。
  * テストを含むこと。
  * 50/70形式のコミットメッセージを使用すること。

### 安全な実践

* リファクタリング中にテストのアサーションを変更しないこと。
* 失敗するテストをスキップしないこと。
* 不明なAPIを勝手に作らないこと。不確かな場合は確認すること。

### プロジェクト設定

* Poetryを使用すること。

### 目標

一貫性があり、安全で、テスト可能で、保守しやすいコードを生産する。
ルールに従うこと——近道はしない。
